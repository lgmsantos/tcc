apply plugin: 'java'
apply plugin: 'eclipse'

sourceCompatibility = 1.7
version = '1.0'

repositories { mavenCentral() }

dependencies { }

test { }

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}
